#### 线性渐变

```
.background(
	.LinearGradien(
		gradient: Gradient(colors: [Color.red, Color,blue]),
		startPoint: .leading,							 // left, can use .top, .bottom and so on
		endPoint: .trailing								 // right
	)
)
```
